public class Arrays {

    // Метод 1: binarySearch для byte[]
    public static int binarySearch(byte[] a, byte key) {
        return binarySearch(a, key);
    }

    // Метод 2: binarySearch для byte[] с fromIndex и toIndex
    public static int binarySearch(byte[] a, int fromIndex, int toIndex, byte key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 3: binarySearch для char[]
    public static int binarySearch(char[] a, char key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 4: binarySearch для char[] с fromIndex и toIndex
    public static int binarySearch(char[] a, int fromIndex, int toIndex, char key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 5: binarySearch для double[]
    public static int binarySearch(double[] a, double key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 6: binarySearch для double[] с fromIndex и toIndex
    public static int binarySearch(double[] a, int fromIndex, int toIndex, double key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 7: binarySearch для float[]
    public static int binarySearch(float[] a, float key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 8: binarySearch для float[] с fromIndex и toIndex
    public static int binarySearch(float[] a, int fromIndex, int toIndex, float key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 9: binarySearch для int[]
    public static int binarySearch(int[] a, int key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 10: binarySearch для int[] с fromIndex и toIndex
    public static int binarySearch(int[] a, int fromIndex, int toIndex, int key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 11: binarySearch для long[]
    public static int binarySearch(long[] a, long key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 12: binarySearch для long[] с fromIndex и toIndex
    public static int binarySearch(long[] a, int fromIndex, int toIndex, long key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 13: binarySearch для short[]
    public static int binarySearch(short[] a, short key) {
        return java.util.Arrays.binarySearch(a, key);
    }

    // Метод 14: binarySearch для short[] с fromIndex и toIndex
    public static int binarySearch(short[] a, int fromIndex, int toIndex, short key) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key);
    }

    // Метод 15: binarySearch для T[] с Comparator
    public static <T> int binarySearch(T[] a, T key, java.util.Comparator<? super T> c) {
        return java.util.Arrays.binarySearch(a, key, c);
    }

    // Метод 16: binarySearch для T[] с fromIndex, toIndex и Comparator
    public static <T> int binarySearch(T[] a, int fromIndex, int toIndex, T key, java.util.Comparator<? super T> c) {
        return java.util.Arrays.binarySearch(a, fromIndex, toIndex, key, c);
    }
}
